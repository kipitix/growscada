info:
  name: get indicator types
  type: http
  seq: 6

http:
  method: GET
  url: http://{{API_SERVER}}/api/v1/indicator-types
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
