info:
  name: delete tag by id
  type: http
  seq: 4

http:
  method: DELETE
  url: http://{{API_SERVER}}/api/v1/tags/{{TAG_ID}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
