info:
  name: get tag by id
  type: http
  seq: 3

http:
  method: GET
  url: http://{{API_SERVER}}/api/v1/tags/{{TAG_ID}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
