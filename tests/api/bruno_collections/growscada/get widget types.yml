info:
  name: get widget types
  type: http
  seq: 6

http:
  method: GET
  url: http://{{API_SERVER}}/api/v1/widget-types
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
