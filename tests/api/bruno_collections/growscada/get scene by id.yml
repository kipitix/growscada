info:
  name: get scene by id
  type: http
  seq: 12

http:
  method: GET
  url: http://{{API_SERVER}}/api/v1/scenes/{{SCENE_ID}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
