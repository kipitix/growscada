info:
  name: tags
  type: http
  seq: 1

http:
  method: GET
  url: http://{{API_SERVER}}/api/v1/tags
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
