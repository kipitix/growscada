info:
  name: get widget by id
  type: http
  seq: 17

http:
  method: GET
  url: http://{{API_SERVER}}/api/v1/scenes/{{SCENE_ID}}/widgets/{{WIDGET_ID}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
