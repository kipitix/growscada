info:
  name: get widgets by scene id
  type: http
  seq: 21

http:
  method: GET
  url: http://{{API_SERVER}}/api/v1/scenes/{{SCENE_ID}}/widgets
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
