info:
  name: post indicator types
  type: http
  seq: 7

http:
  method: POST
  url: http://{{API_SERVER}}/api/v1/indicator-types
  body:
    type: json
    data: |-
      {
        "name": "pressure gauge",
        "svg_template": "<svg xmlns='http://www.w3.org/2000/svg'><circle cx='50' cy='50' r='40'/></svg>",
        "script": "function render(value) { return value; }",
        "script_language": "javascript"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
