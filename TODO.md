# Задачи по проекту

## Backlog

### Агрегат Library

- Сейчас библиотека корневая сущность приложения и по факту не существует
- Нужно создать такой агрегат и сделать дочерними агрегатами WidgetType, ComputationType, Theme
- Нужно добавить это в UI (например как отдельные вкладки во вкладке Library)

### Агрегат Theme

- Содержит в себе названия цветов, значений толщины, разметов и т.д.
- Является дочерним элементов по отношению к Library (можно сделать и без Library, но тогда надо, чтобы это было в UI во вкладке Library)

### Сборка проекта в один исполняемый файл!!! (killer feature)

### Выделение и перемещение нескольких объектов на Project

### Направляющие при редактировании сцен

### Убрать тип качества Simulated

### Агрегат Device

`Device` - источник значений тегов.
Нужно как-то декларировать какому `Device` относится тот или иной тег (`Tag`).
Нужно как-то проверять авторизацию `Device` по токену.
Нужен механизм выдачи токенов устройствам.

### Протокол между клиентом и сервером для передачи состояний

### Запись и воспроизведение событий

### Агрегат Command

### Агрегат Alert

### Функционал выдачи звуков на клиенте

- Повторяющиеся звуки по состоянию
- Одиночные (задаваемое число повторений) звуки по событию

### Функционал наведения пользователем мышью на объекте

- Записывать в историю это событие
- Передавать с сервера возможность подсветки этого объекта
- Воспроизводить в режиме History

### Агрегат ComputationType и Computation

- Computation - некое вычисление, производимое на сервере
- Цель - сделать обработку и формирование новых тегов (например из нескольких значений сформировать одно)
- Нужна работа со временем (таймеры)
- Нужно срабатывание по таймеру или по изменению одно из тегов, из которых формируется значение
- Нужна возможность помнить некоторые состояния между вызовами
- Нужна возможность обрабатывать команды от пользователя

### Агрегат Project

- Сейчас возможен 1 проект, но нужно сделать сервер для использования нескольких проектов

### Агрегат User

- Пользователю принадлежат библиотеки и проекты

### Симулятор устройства нижнего уровня

- Разработать симулятор нижнего уровня, который бы управлялся по своему API для имитации и тестов
- Добавить интеграционные автотетсы
- Сделать автозапуск в CI github и gitverse

### Права доступа

- Разграничение пользователей и устройств
- Для пользователей авторизация через внешние системы
- Для устройств авторизация через service account

### Интеграционные тесты приложения через bruno

- Добавить коллекции запросов в bruno для базовых тестов API
- Сделать сборку приложения в контейнере
- Сделать автозапуск в CI github и gitverse

## Done

### Базовая логика агрегата tag

- VO, ET, AG для tag
- Репозиторий tag на postgres
- RestAPI для tags
- Коллекция bruno для тестирования

### Юнит-тесты доменного слоя

- Добавть юнит-тесты для агрегата tag
- Исправить баги по необходимости
- Сделать автозапуск в CI github и gitverse

### Юнит-тесты инфраструктурного слоя

- Добавить юнит-тесты для репозитория агрегата tag
- Использовать testcontainers для запуска СУБД (чтобы не писать mock)
- Сделать автозапуск в CI github и gitverse

### Юнит-тесты слоя интерфейса (API)

- Добавить юнит-тесты для сервиса tag
- Добавить юнит-тесты для restapi агрегата tag
- Запускать в связке с инфраструктурным слоем - практически целое приложение
- Использовать testcontainers для запуска СУБД (чтобы не писать mock)
- Сделать автозапуск в CI github и gitverse

### Удаление и обновление значений тегов

- Добавить метод обновления тега из нижнего уровня
- Добавить метод удаления тега пользователем
- Покрыть тестами

### События о создании, обновлении и удалении тега

- Разработать классы брокера, самих сообщений
- Добавить выдачу событий на уровень прикладного сервиса
- Реализовать выдачу событий в брокер сообщений (kafka?)

### Рефакторинг DTO Tag (перенос их на уровень интерфейса)

- Перенести DTO на уровень интерфейса
- Сделать рефакторинг сервиса уровня приложения TagService, чтобы он не зависел от текущих DTO (сделать ещё группу DTO)?

### Агрегат WidgetType

- Разработать агрерат, репозиторий, сервис, REST API, UI
- Тесты

### Агрегат Widget

- Разработать агрегат, репозиторий, сервис, REST API, UI
- Разработать агрегат сцены, на котором размещать виджеты
- Тесты

### Доработка WidgetType - добавление InputPort

- VO `InputPort`
- Придумать как правильно ссылаться на `InputPort` (по ID или тут будет правильнее по имени?)
- Миграции в базе
- Корректировка примеров (сделать нормальные, понятные и красивые WidgetType)
- Доработать UI во вкладках Library и Project
- Покрыть всё юнит-тестами

### Нормальные примеры WidgetType

- Тип отображающий bool (какой-то индикатор, например лампочка)
- Тип отображающий int (индикатор температуры)
- Тип отображающий строку (например IP адрес устройства, датчика и т.д.)
